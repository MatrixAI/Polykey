// import os from 'os'
// import fs from 'fs'
// import { keysModel } from './KeyManager.model';
// import { KeyManager } from '../../../src/Polykey';
// import { randomString } from '../../../src/utils';


describe('KeyManager spec testing', () => {
//   const testPlans = keysModel.getSimplePathPlans();

//   testPlans.forEach((plan) => {
//     describe(plan.description, () => {
//       plan.paths.forEach((path) => {
//         test(path.description, async () => {
//           const tempDir = fs.mkdtempSync(`${os.tmpdir}/pktest${randomString()}`)
//           const km = new KeyManager(tempDir, fs)
//           await path.test(km);
//         });
//       });
//     });
//   });

  test('should have full coverage', () => {
//     return keysModel.testCoverage();
  });
});
