// import { peerDHTModel, createMockPeerDHT } from './PeerDHT.model';

describe('PeerDHT spec testing', () => {
//   const testPlans = peerDHTModel.getSimplePathPlans();

//   testPlans.forEach((plan) => {
//     describe(plan.description, () => {
//       plan.paths.forEach((path) => {
//         test(path.description, async () => {
//           const peerDHT = await createMockPeerDHT()
//           await path.test(peerDHT);
//         });
//       });
//     });
//   });

  test('should have full coverage', () => {
//     return peerDHTModel.testCoverage();
  });
});
