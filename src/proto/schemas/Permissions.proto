syntax = "proto3";

import "Nodes.proto";
import "Identities.proto";

package permission;

message Actions {
  repeated string action = 1;
}

message ActionSet {
  oneof node_or_provider {
    node.Node node = 1;
    identity.Provider identity = 2;
  }
  string action = 3;
}
